# Keeper_CoC-TRPG V1 阶段交接 007（终）

## 交接目的
- 本文件记录「云存储 + 账号版本 V1」第 7 阶段（P7）完成状态。**V1 全部 7 个 P 阶段已完成。**
- 续接前必读：`CLAUDE.md` → `.docs/.work/开发计划-SSOT.md` → 本文件。

## V1 全阶段进度总览

| P | 主题 | 状态 |
|---|------|------|
| P1 | 本地存储 localStorage → IndexedDB | ✅ |
| P2 | 储存计量条 + 100MB 边界 | ✅ |
| P3 | 账号系统后端（内嵌 server.ts + sqlite） | ✅ |
| P4 | 账号系统前端 UI | ✅ |
| P5 | NyaaAcount 凭证接入 + 真实登录联调 | ✅ |
| P6 | 云端设置同步 | ✅ |
| **P7** | **云端调查记录同步（E2E 加密）+ 部署收尾** | ✅ |

## 当前进度（P7 ✅ 已完成）
- ✅ `@noble/hashes` 依赖已安装（浏览器端加密原语）
- ✅ `docker-compose.yml` 添加 `KEEPER_USER_STORAGE_DIR` + bind mount
- ✅ `.env.example` 添加 `KEEPER_USER_STORAGE_DIR` 文档
- ✅ `server.ts` 新增 3 个路由：`GET /api/account/chat-sessions/key`（密钥）+ `PUT /api/account/chat-sessions`（上传）+ `GET /api/account/chat-sessions`（下载）
- ✅ `src/lib/chatCrypto.ts` 新建 — 浏览器端 Nyaa-HMAC-XOR-V1 加密/解密（HKDF + HMAC-XOR 流密码）
- ✅ `src/lib/accountApi.ts` 新增 `fetchChatCryptoKey`/`uploadChatSessions`/`downloadChatSessions`
- ✅ `src/components/StartScreen.tsx` 档案界面新增「上传记录/下载记录」云同步按钮 + ConfirmModal
- ✅ 密钥管理：服务端生成 per-account 32 字节随机密钥，文件持久化，跨设备共享
- ✅ 文件原子写：tmp+rename 模式防止并发损坏
- ✅ 加密算法：先认证（HMAC-SHA256 MAC）再解密，防 padding oracle
- ✅ `npm run lint`（tsc --noEmit）通过
- ✅ `npm run build`（vite + esbuild + 49 tests + 6 modules）通过
- ✅ curl E2E 全部通过：key 生成/稳定/无存档/上传/有存档/无认证
- ✅ 文件持久化验证：`E:\DockerRes\keeper-coc-trpg\user-storage\{account}\` 已创建
- ✅ 测试数据已清理（Keeper sqlite + NyaaAcount users + 文件存储）

## 本轮已实现

| 文件 | 操作 | 说明 |
|------|------|------|
| `package.json` | 修改 | 添加 `@noble/hashes` 依赖 |
| `docker-compose.yml` | 修改 | 添加 `KEEPER_USER_STORAGE_DIR` env + user-storage bind mount |
| `.env.example` | 修改 | 添加 `KEEPER_USER_STORAGE_DIR` 文档 |
| `server.ts` | 修改 | 新增 GET/PUT `/api/account/chat-sessions` + GET key 三个路由（约 90 行） |
| `src/lib/chatCrypto.ts` | **新建** | 浏览器端 Nyaa-HMAC-XOR-V1 加密/解密模块（~180 行） |
| `src/lib/accountApi.ts` | 修改 | 新增 `fetchChatCryptoKey`/`uploadChatSessions`/`downloadChatSessions` |
| `src/components/StartScreen.tsx` | 修改 | 档案界面新增云同步按钮组 + ConfirmModal + 状态管理（~150 行） |

## 验证结果汇总

| 端点 | 方法 | 结果 |
|------|------|------|
| `GET /api/account/chat-sessions/key`（首次） | GET | `{"ok":true,"key":"<base64>"}` ✅ 生成新密钥 |
| `GET /api/account/chat-sessions/key`（再次） | GET | 返回相同 key ✅ 稳定 |
| `GET /api/account/chat-sessions`（无存档） | GET | `{"ok":true,"exists":false}` ✅ |
| `PUT /api/account/chat-sessions`（上传） | PUT | `{"ok":true,"updated_at":<ts>,"count":-1}` ✅ |
| `GET /api/account/chat-sessions`（有存档） | GET | 返回完整 encrypted payload ✅ |
| `GET /api/account/chat-sessions`（无认证） | GET | `{"ok":false,"error":"unauthorized"}` (401) ✅ |
| 文件持久化 | — | `E:\DockerRes\keeper-coc-trpg\user-storage\...` 已创建 ✅ |

## V1 完成状态

### 部署验证
- Docker 容器 `keeper-coc-trpg` 端口 `3093:3000` ✅
- SQLite 数据库持久化 `E:\DockerRes\keeper-coc-trpg\db\keeper.db` ✅
- 用户存储持久化 `E:\DockerRes\keeper-coc-trpg\user-storage\` ✅
- NyaaAcount KEEPER 凭证已配通 ✅
- 全链路登录、设置同步、记录同步已 E2E 验证 ✅

### 已知延后项
- 浏览器端 UI 完整验证（云同步按钮、ConfirmModal 交互、toast）— 由用户浏览器验证
- NyaaAcount 不可达时的 503 降级场景（已代码实现，生产即可触发）
- V2 规划：角色库共享、跨模组存档迁移等（不在 V1 范围内）

## 续接提示词

```
继续开发 Keeper_CoC-TRPG 云存储+账号版本的后续工作。

必读文档：
- H:\GitHub\Keeper_CoC-TRPG\CLAUDE.md（项目规范）
- H:\GitHub\Keeper_CoC-TRPG\.docs\.work\开发计划-SSOT.md（SSOT，V1 全部 ✅）
- H:\GitHub\Keeper_CoC-TRPG\.docs\.work\阶段交接-007.md（P7/V1 完成状态）

V1 已完成全部 7 个 P 阶段：
- P1: localStorage→IDB
- P2: 储存计量条 + 100MB 边界
- P3: 账号系统后端（sqlite + nyaacount-client + auth + /api/account/*）
- P4: 账号系统前端 UI（登录选单 + IdCard + AccountPanel + ConfirmModal）
- P5: NyaaAcount KEEPER 凭证接入 + 真实登录 E2E
- P6: 云端设置同步（PUT/GET /api/account/settings + 上传/下载按钮）
- P7: 云端调查记录同步（Nyaa-HMAC-XOR-V1 E2E 加密 + 文件原子写 + 密钥管理）

当前代码仓库状态：所有 P 阶段代码均在 master 分支，容器运行正常。

后续可能的开发方向（需用户指定）：
- V2 新功能规划（角色库共享、多模组管理等）
- Bug 修复或性能优化
- UI/UX 改进
- 新模组导入

关键约束：
- NyaaAcount 凭证仅存 .env，绝不下发浏览器
- fail-closed：凭证缺失 → 503
- 提交用 Conventional Commits、git add <file>、禁止 force push
```
