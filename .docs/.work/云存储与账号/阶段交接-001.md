# Keeper_CoC-TRPG V1 阶段交接 001

## 交接目的
- 本文件记录「云存储 + 账号版本 V1」第 1 阶段（P1）完成状态，供下一阶段和新对话续接。
- 续接前必读：`CLAUDE.md`（项目根） → `.docs/.work/开发计划-SSOT.md` → 本文件。

## 当前进度（P1 ✅ 已完成）
- ✅ 新建 `src/lib/idbStorage.ts` — 原生 IndexedDB KV 封装
- ✅ `src/lib/saveManager.ts` — 缓存 + hydrate + 异步 IDB 落盘，对外签名不变
- ✅ `src/lib/apiSettings.ts` — 同上
- ✅ `src/main.tsx` — bootstrap 加入 migrate → hydrate → render 链
- ✅ `npm run lint`（tsc --noEmit）通过
- ✅ `npm run build`（vite + esbuild + validator 49 tests + 6 modules）通过

## 本轮已修复 / 已实现

| 文件 | 改动 |
|---|---|
| `src/lib/idbStorage.ts`（新增） | 原生 IDB KV 封装：`getItem/setItem/removeItem/getAllKeys/isMigrationDone/migrateFromLocalStorage`。`DB_NAME="keeper_storage"`，store `kv`，哨兵幂等迁移。 |
| `src/lib/saveManager.ts` | 加 `cache + hydrateSaves() + ensureCache() + persist()`。`getAllSaves/saveGame/deleteSave` 改为先改缓存再 fire-and-forget 异步落盘。对外 API 签名和行为语义不变。 |
| `src/lib/apiSettings.ts` | 加 `cache + hydrateApiSettings() + ensureCache() + persist()`。`loadApiSettings/saveApiSettings` 同上。 |
| `src/main.tsx` | render 前 async 链路：`loadPublicConfig → migrateFromLocalStorage() → hydrateSaves() + hydrateApiSettings() → render(App)`。 |
| `.docs/.work/开发计划-SSOT.md`（新增） | V1 完整 SSOT 计划书（7 个 P 阶段 / 架构决策 / 环境变量约定 / 执行守则）。 |
| `.docs/.work/云存储与账号版本初始设计.md` | 用户提供的初始设计文档（跟踪入仓）。 |

## 仍需继续验证 / 已知问题
- IDB 迁移在浏览器实际环境中尚未端到端测试（需 rebuild + 浏览器验证）。
- 老 localStorage 数据迁移后原有数据仍在 localStorage 中直到迁移成功（幂等哨兵机制，安全）。
- `App.tsx` / `StartScreen.tsx` 无需改动——对 `getAllSaves/saveGame/deleteSave/loadApiSettings/saveApiSettings` 的调用签名和行为语义完全不变。

## 下一阶段（P2）
- P2：储存计量条 + 100MB 边界限制（`storageEstimate.ts` + `StorageBar` 组件 + 导入/保存守卫）。
- P2 在 P1 基础上加组件，不依赖账号系统，可独立进行。

## 续接提示词
```
继续开发 Keeper_CoC-TRPG 云存储+账号版本 V1 的 P2 阶段：储存计量条 + 100MB 边界限制。

必读文档：
- H:\GitHub\Keeper_CoC-TRPG\CLAUDE.md（项目规范）
- H:\GitHub\Keeper_CoC-TRPG\.docs\.work\开发计划-SSOT.md（V1 完整蓝图、架构决策）
- H:\GitHub\Keeper_CoC-TRPG\.docs\UI-STYLE-GUIDE.md（配色/控件铁律）

当前进度：
- P1 已完成：本地存储已从 localStorage 迁移到 IndexedDB（缓存+hydrate+异步落盘模式），迁移幂等哨兵驱动。新增 src/lib/idbStorage.ts。
- 对外 API 签名不变：getAllSaves/saveGame/deleteSave/loadApiSettings/saveApiSettings 行为语义与原来一致，调用者无需改动。

P2 要做的：
- 新建 src/lib/storageEstimate.ts：CHAT_STORAGE_QUOTA=100MB，estimateSaveStorage() 按存档 JSON 字节手工计量
- 新建 src/components/StorageBar.tsx 计量条组件（遵循 Keeper 配色/控件规范：三色铁律、custom-scrollbar、#10b981 绿）
- 在 StartScreen.tsx「调查记录档案」界面上方或底部加入计量条
- 在存档导入（handleFileUpload）和自动保存 effect 加入容量守卫：超限拒绝/提示
- 验证：npm run lint && npm run build 全绿。如需 rebuild + 浏览器验证也走一遍。
- 完成后写 .docs/.work/阶段交接-002.md

关键约束：
- 遵守 UI-STYLE-GUIDE.md 三色铁律（黑绿白，#10b981）
- 所有滚动容器必须挂 custom-scrollbar
- 不新增第三方依赖（IDB 层零依赖，与 NyaaChat 一致）
- 提交用 Conventional Commits、git add <file>、禁止 force push
```
