# Keeper_CoC-TRPG V1 阶段交接 006

## 交接目的
- 本文件记录「云存储 + 账号版本 V1」第 6 阶段（P6）完成状态，供下一阶段和新对话续接。
- 续接前必读：`CLAUDE.md` → `.docs/.work/开发计划-SSOT.md` → 本文件。

## 当前进度（P6 ✅ 已完成）
- ✅ `db.ts` 新增 `getUserSettings` + `upsertUserSettings` prepared statements
- ✅ `server.ts` 新增 `PUT /api/account/settings`（上传）和 `GET /api/account/settings`（下载）
- ✅ `accountApi.ts` 新增 `uploadCloudSettings` / `downloadCloudSettings` / `buildSettingsExportPayload`
- ✅ `ApiSettingsPanel.tsx` 新增云同步按钮组（上传设置/下载设置）+ ConfirmModal 二次确认
- ✅ 上传：预检查云端存档 → ConfirmModal（default）显示时间戳 → 覆盖式 upsert
- ✅ 下载：获取云端存档 → ConfirmModal（danger 红色）显示时间戳 → 覆盖本地设置并自动保存
- ✅ 未登录时点击云同步按钮 → toast 提示"请先登录调查员账户"
- ✅ `npm run lint`（tsc --noEmit）通过
- ✅ `npm run build`（vite + esbuild + 49 tests + 6 modules）通过
- ✅ curl E2E 全部通过：GET(无存档)→PUT(上传)→GET(有存档)→PUT(无认证401)→PUT(错误格式400)
- ✅ 测试数据已清理

## 本轮已实现

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/lib/db.ts` | 修改 | 新增 `getUserSettings` + `upsertUserSettings` prepared statements |
| `server.ts` | 修改 | 新增 import + PUT/GET `/api/account/settings` 路由（requireAuth） |
| `src/lib/accountApi.ts` | 修改 | 新增 `CloudSettingsResponse` + `uploadCloudSettings`/`downloadCloudSettings`/`buildSettingsExportPayload` |
| `src/components/ApiSettingsPanel.tsx` | 修改 | 新增云同步按钮组 + 3 个处理函数 + 2 个 ConfirmModal（upload default / download danger）|

**无新建文件。** 4 个现有文件增量改动。

## 验证结果汇总

| 端点 | 方法 | 结果 |
|------|------|------|
| `GET /api/account/settings`（无存档） | GET | `{"ok":true,"exists":false}` ✅ |
| `PUT /api/account/settings`（上传） | PUT | `{"ok":true,"updated_at":<ts>}` ✅ |
| `GET /api/account/settings`（有存档） | GET | `{"ok":true,"exists":true,"updated_at":<ts>,"payload":{...}}` ✅ |
| `PUT /api/account/settings`（无认证） | PUT | `{"ok":false,"error":"unauthorized"}` (401) ✅ |
| `PUT /api/account/settings`（错误格式） | PUT | `{"ok":false,"error":"bad_payload"}` (400) ✅ |

## 仍需继续验证 / 已知问题
- 浏览器端 UI 验证（云同步按钮位置、ConfirmModal 交互、toast 提示）— 由用户在浏览器中自行验证
- 导出格式当前为 `_version: 1`，后续如需向后兼容可扩展 `validateApiSettingsJson` 到 payload 级别

## 下一阶段（P7）
- P7：云端调查记录同步（E2E 加密）+ 部署收尾 — 档案界面「下载记录/上传记录」按钮；Nyaa-HMAC-XOR-V1 加密；文件原子写 temp+rename；docker-compose bind mount 验证

## 续接提示词

```
继续开发 Keeper_CoC-TRPG 云存储+账号版本 V1 的 P7 阶段：云端调查记录同步（E2E 加密）+ 部署收尾。

必读文档：
- H:\GitHub\Keeper_CoC-TRPG\CLAUDE.md（项目规范）
- H:\GitHub\Keeper_CoC-TRPG\.docs\.work\开发计划-SSOT.md（§5.5 云同步方案）
- H:\GitHub\Keeper_CoC-TRPG\.docs\.work\阶段交接-006.md（P6 完成状态）

当前进度：
- P1 已完成：localStorage→IDB
- P2 已完成：储存计量条 + 100MB 边界
- P3 已完成：账号系统后端
- P4 已完成：账号系统前端 UI
- P5 已完成：NyaaAcount KEEPER 凭证接入 + 真实登录 E2E
- P6 已完成：云端设置同步（PUT/GET /api/account/settings + 上传/下载按钮 + ConfirmModal 二次确认）
- 设置同步链路完整可用：前端按钮 → accountApi → server.ts → sqlite user_settings（upsert）

P7 要做的：
- 后端新增 PUT/GET /api/account/chat-sessions（文件原子写 temp+rename）+ GET /api/account/chat-sessions/key
- 前端「调查记录档案」界面新增「下载记录/上传记录」按钮
- 客户端 Nyaa-HMAC-XOR-V1 加密（chatCrypto），服务端持有密钥跨设备共享
- docker-compose bind mount 验证 + 部署收尾

关键约束：
- 加密算法复用 Nyaa-HMAC-XOR-V1（Node crypto 版），与 P5 凭证传输同源
- 服务端密钥存 sqlite 或 env，跨设备共享
- 文件原子写 temp+rename 防止并发损坏
- 提交用 Conventional Commits、git add <file>、禁止 force push
```
